package com.example.pomeserver.domain.record.repository;

import com.example.pomeserver.domain.record.entity.HideRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HideRecordRepository extends JpaRepository<HideRecord, Long> {
}
